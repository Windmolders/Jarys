<div class="jarys-navbar">
    <div class="container">
        <nav class="navbar">
            <a class="navbar-brand" href="{{ path('homepage') }}">
                <img src="{{ asset('assets/images/logo/jarys.png') }}" />
            </a>
            <ul class="nav navbar-nav pull-right">
                {{ jarys_clastic_menu('main') }}
            </ul>
        </nav>
    </div>
</div>
